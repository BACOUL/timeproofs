import type { MetadataRoute } from "next";

const SITE = process.env.NEXT_PUBLIC_SITE_URL ?? "https://timeproofs.io";
const lastModified = new Date();

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    { url: `${SITE}/`, lastModified, priority: 1.0 },
    { url: `${SITE}/docs`, lastModified, priority: 0.9 },
    { url: `${SITE}/verify`, lastModified, priority: 0.9 },
    { url: `${SITE}/privacy`, lastModified, priority: 0.6 },
    { url: `${SITE}/legal`, lastModified, priority: 0.6 },
  ];
}
