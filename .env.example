# =========================================
# TimeProofs — Example environment file
# Copy to ".env" for local *server/worker* dev only.
# Do NOT commit your real .env with secrets.
# =========================================

# -------- FRONTEND (public) --------
# Public website base URL for building absolute links (no secret)
PUBLIC_BASE_URL=https://timeproofs.vercel.app

# Edge API base used by the docs/website (also public)
API_BASE_URL=https://timeproofs-api.jeason-bacoul.workers.dev/api

# Optional, privacy-first analytics domain (leave empty to disable)
PLAUSIBLE_DOMAIN=

# -------- WORKER (server-side ONLY) --------
# HMAC secret used to sign (hash + timestamp). Keep PRIVATE.
# In production, set this in Cloudflare with: wrangler secret put TIMEPROOFS_SECRET
TIMEPROOFS_SECRET=replace_me_locally_only

# Optional Stripe keys (v1.0+) — keep SECRET server-side
# In production, set these in Cloudflare as secrets (not in .env):
STRIPE_PUBLIC_KEY=pk_test_replace_me
STRIPE_SECRET_KEY=sk_test_replace_me

# Cloudflare KV namespace binding (must match wrangler.toml)
CF_KV_NAMESPACE=timeproofs_kv
