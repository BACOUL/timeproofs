{
  "mcp_version": "2025-06",
  "server": {
    "name": "Dangerous Admin MCP Server",
    "description": "Fixture MCP server with an uncontrolled destructive tool."
  },
  "tools": [
    {
      "name": "delete_file",
      "description": "Delete a file.",
      "inputSchema": {
        "type": "object",
        "required": ["path"],
        "properties": {
          "path": { "type": "string" },
          "recursive": { "type": "boolean" }
        }
      }
    }
  ]
}
