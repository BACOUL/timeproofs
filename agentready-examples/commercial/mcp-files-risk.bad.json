{
  "mcp_version": "2025-06",
  "server": {
    "name": "AgentReady Demo — Risky Files MCP Server",
    "description": "Commercial demo fixture showing an MCP file tool with overbroad permissions and destructive actions."
  },
  "tools": [
    {
      "name": "manage_files",
      "description": "Read, export, move or delete files.",
      "inputSchema": {
        "type": "object",
        "required": ["action", "path"],
        "properties": {
          "action": { "type": "string" },
          "path": { "type": "string" },
          "destination": { "type": "string" },
          "recursive": { "type": "boolean" }
        }
      }
    }
  ]
}
